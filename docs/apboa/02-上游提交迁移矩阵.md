# 上游 Apboa 提交迁移矩阵

## 范围

本文记录 `.apboa` 从父仓库原始指针 `12ff55f` 到当前工作区指针 `2cafdc8` 的提交内容，及其在新工程中的迁移状态。

`.apboa` 仍是只读参考源码。迁移实现只落在 `source/agent-platform/`、`source/agent-platform-ui/` 或 `docs/`。

## 当前指针

- 父仓库记录的 `.apboa`：`12ff55f`
- 当前 `.apboa` checkout：`2cafdc8`
- `.apboa` 远端 `origin/master`：`01f632f`
- 说明：`2cafdc8` 是本地 merge commit，包含 `origin/master` 的 Docker 构建修复。

## 覆盖状态

| 上游主题 | 代表提交 | 迁移状态 | 新工程落点 |
| --- | --- | --- | --- |
| Redis 消息发布与订阅优化 | `54f0166`, `7ae58d7` | 已迁入，已补单测 | `agent-run/run-event`、调用方已使用 `publishAfterCommit` |
| 模型连接性检测 | `24c3aca`, `d0b9245`, `748fd4a`, `ea9b848` | 已迁入；新工程采用表格弹窗版，不引入上游卡片组件 | `adapter-rest`、`runtime-agentscope`、`api-capability`、`agent-platform-ui` |
| 聊天消息复制 | `c90e207` | 已迁入 | `source/agent-platform-ui/src/components/chat/MessageItem.vue` |
| Skill 文件粒度管理 | `89b5903`, `051252a`, `06c14b4`, `e2a49dc`, `d1d7c34`, `5d15845`, `8c93f2f` | 已迁入，已补分类设置 UI 与 e2e 覆盖 | `capability-skill`、`adapter-rest`、`repo-spi`、`repo-mybatis`、`domain-*`、`V7__apboa_20260601_model_and_skill_files.sql`、`agent-platform-ui` |
| README / 使用手册 / 图片资源 | `71996ca`, `d5169b9`, `bb59989` | 已按新工程文档体系迁移，不直接搬旧 README | `docs/apboa/03-上游文档资源迁移说明.md`、`docs/apboa/assets`、`source/agent-platform-ui/public/images` |
| Elasticsearch 向量存储 | `c7ddfff`, `d7a4fa1` | 本轮迁入 | `capability-rag`、`repo-vector`、`application-dev.yml` |
| 向量库 Docker profile 与外置 ES | `ddda622`, `125dc50`, `8016e74`, `60e614c` | 已按新工程结构重建 Docker，含 ES/Weaviate profile | `.dockerignore`、`docker/`、`application-docker.yml` |
| 默认登录密码格式兼容 | `ff66eee` | 已迁入，已补单测 | `governance-iam` / `governance-auth` |
| AgentSession 数据库名探测 | `c35daee`, `dc0e990`, `6cf31f2`, `cea0e53` | 新工程不适用，已补 registry 缺失日志 | 新工程使用 `PostgresSession(DataSource, tableName)`，不再向 session store 传 database name |
| Weaviate RAG | `f2f1222`, `a0d16e2` | 本轮迁入 | `capability-rag`、`repo-vector`、`application-dev.yml` |
| 存储配置异常提示 | `3f895bb`, `31eed6c` | 已迁入，已补单测 | `worker-file` |
| Skill 注册和代码执行环境前置 | `8837368`, `932fa87` | 已迁入，已补单测 | `runtime-agentscope`、`worker-core` |
| Docker 构建上下文与后端启动 | `eabb4ba`, `01f632f`, `2cafdc8` | 已按新工程结构重建 Docker，并通过 compose config 静态校验 | `.dockerignore`、`docker/backend/Dockerfile`、`docker/frontend/Dockerfile`、`docker/docker-compose.yml` |

## 本轮已完成

- 新增 Elasticsearch / Weaviate 客户端配置。
- 新增 Elasticsearch / Weaviate `VectorStore` 实现。
- 新增无需外部向量库服务的单元测试。
- 补充 `application-dev.yml` 向量库配置样例。
- 补齐 Redis after-commit 发布、监听线程池参数和重试单测。
- 补齐默认登录密码兼容、存储配置异常文案、SkillBox 注册时序回归测试。
- 补齐 Skill 卡片“设置分类”前端交互，并用 Playwright e2e 覆盖分类设置、Skill 文件编辑和模型连接检测。
- 新增新工程 Dockerfile、Compose、Nginx 代理和 `docker` Spring profile，Compose 静态校验通过。
- 迁移上游新增文档图片资源，补充文档资源迁移说明。
- 完成全量验证：`mvn -q test`、`pnpm run build:main`、`pnpm run e2e`。

## 后续迁移检查点

- 提交前继续保持 .apboa 只读，不暂存 .apboa gitlink；父仓库指针差异仅用于迁移范围记录。
- 若后续继续沿用上游使用手册图片，需要在新前端文档页按新导航结构重新引用。

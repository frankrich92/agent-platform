# 工程迭代索引

本目录保存 V3 的小工程交付内容。顶层 V3 文档只描述长期目标、全局候选基线和通用规则；真实评审、实现、验收应优先进入具体小工程目录。

当前小工程：

```text
01-chat-minimal
```

目录约定：

- 每个小工程有自己的范围、契约切片、技术设计、测试策略、路线图和任务包。
- 每个小工程的真实 ADR 放在该工程的 `adr/` 目录。
- 共享模板放在 `docs/v3-agent中台/templates/`。
- 新小工程启动前，必须先从全局候选基线抽取自身 contract slice。
